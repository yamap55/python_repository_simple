name: black

on: [push]

jobs:
  black:
    uses: yamap55/python_repository_simple/.github/workflows/_python_setup.yml@update/reusing_workflows
    steps:
      # - uses: ./.github/workflows/_python_setup.yml@update/reusing_workflows
      # - uses: actions/checkout@v2
      # - name: Set up Python 3.9
      #   uses: actions/setup-python@v2
      #   with:
      #     python-version: 3.9
      # - name: Install Poetry
      #   uses: snok/install-poetry@v1.3
      #   with:
      #     virtualenvs-create: false
      # - uses: actions/cache@v2
      #   id: cached-poetry-dependencies
      #   with:
      #     path: ~/.cache/pypoetry
      #     key: ${{ runner.os }}-poetry-${{ hashFiles('**/poetry.lock') }}
      #     restore-keys: |
      #       ${{ runner.os }}-poetry-
      # - name: Install dependencies
      #   if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
      #   run: poetry install --no-interaction
      - name: Run black with review dog
        uses: reviewdog/action-black@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-check
          black_args: "--diff --line-length 100"
          fail_on_error: true
